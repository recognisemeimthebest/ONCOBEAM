# ml_models/

학습이 끝난 모델 파일(`.joblib`)을 여기에 둡니다. 서버가 시작될 때 자동으로 불러옵니다.

기대하는 파일 이름:

- `hte_model.joblib` — HTE(처치효과) 모델
- `prognosis_model.joblib` — 예후예측 모델

이 파일이 없으면 백엔드는 **더미(가짜) 예측값**을 반환합니다. 즉, 모델이 아직 없어도 서버는 정상 동작합니다.

모델은 `../../ml_training/` 에서 학습한 뒤 여기로 저장하세요.
